int iscntrl(int argument) {
	if ( (argument < 32) || (argument > 126) ){
		return 1;
	}

	return 0;
}

int main(int argc, char** argv) {
	char c;
	while (c = cgetc() && c != 'q') {
		if (iscntrl(c)) {
			printf("%d\n", c);
		} else {
			printf("%d ('%c')\n", c, c);
		}
	}
	return 0;
}
